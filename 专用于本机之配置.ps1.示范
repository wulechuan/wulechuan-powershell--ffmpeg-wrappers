${global:ffmpeg可执行程序的完整路径} = 'D:\Users\公用\ffmpeg-2022-04-07\bin\ffmpeg.exe'
${global:位于用户桌面的_默认用于存放产生的文件的_文件夹的名称} = 'ffmpeg-产生的录像和录音'





Write-Host "ffmpeg可执行程序的完整路径："
Write-Host -No           "    '"
Write-Host -No -F 'Blue' "${global:ffmpeg可执行程序的完整路径}"
Write-Host               "'"
Write-Host

Write-Host "位于用户桌面的、默认用于存放产生的文件的文件夹的名称："
Write-Host -No           "    '"
Write-Host -No -F 'Blue' "${global:位于用户桌面的_默认用于存放产生的文件的_文件夹的名称}"
Write-Host               "'"
Write-Host
